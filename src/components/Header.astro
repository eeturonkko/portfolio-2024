<!-- Header for larger screens-->
<header
class="md:flex md:flex-wrap md:w-full md:border-b hidden sticky top-0 bg-white z-50"
>
<div class="flex items-center justify-center flex-1 h-24">
  <h2 class="text-4xl font-semibold">
    <span class="text-red-700">E</span>RWD.
  </h2>
</div>
<div
  class="flex-1 h-24 bg-gray-800 flex items-center justify-center space-x-4 animate-clip-path"
>
  <nav class="flex space-x-4 font-semibold text-lg">
    <a href="#about" class="text-white hover:text-gray-300"
      >About</a
    >
    <a href="#skills" class="text-white hover:text-gray-300"
      >Skills</a
    >
    <a href="#portfolio" class="text-white hover:text-gray-300"
      >Portfolio</a
    >
    <a href="#contact" class="text-white hover:text-gray-300"
      >Contact</a
    >
  </nav>
</div>
</header>

<!-- Header for smaller screens -->
<header
class="flex flex-col flex-wrap w-full border-b md:hidden sticky top-0 z-50"
>
<div class="flex items-center justify-center flex-1 h-24 bg-white">
  <h2 class="text-xl font-semibold">
    <span class="text-red-700">E</span>RWD.
  </h2>
</div>
<div
  class="flex-1 h-24 bg-gray-800 flex items-center justify-center space-x-4 py-4"
>
  <nav class="flex space-x-4 font-semibold text-md">
    <a href="#about" class="text-white "
      >About</a
    >
    <a href="#skills" class="text-white "
      >Skills</a
    >
    <a href="#portfolio" class="text-white "
      >Portfolio</a
    >
    <a href="#contact" class="text-white "
      >Contact</a
    >
  </nav>
</div>
</header>
<style>
@keyframes clipPathAnimation {
  from {
    clip-path: polygon(0% 0, 100% 0, 100% 100%, 0% 100%);
  }
  to {
    clip-path: polygon(5% 0, 100% 0, 100% 100%, 0% 100%);
  }
}
@keyframes clipPathAnimationAlt {
  from {
    clip-path: polygon(0 0, 100% 0, 100% 100%, 0% 100%);
  }
  to {
    clip-path: polygon(0 0, 80% 0, 100% 100%, 0% 100%);
  }
}
.animate-clip-path {
  animation: clipPathAnimation 0.6s ease-out forwards;
}

.animate-clip-path-alt {
  animation: clipPathAnimationAlt 0.6s ease-out forwards;
}
</style>